import React from 'react';
import {
    action
} from '@storybook/addon-actions';
import Map from '../components/map';

export default {
    title: 'Map',
    component: Map,
};

export const map = () => ( <
    Map / >
);